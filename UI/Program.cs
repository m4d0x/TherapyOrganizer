namespace UI;
using Logic;
using Spectre.Console;

internal class Program
{
    private static void Main(string[] args)
    {
        bool isRunning = true;
       
        while (true)
        {
            MainUI.MainMenu();
        }
    }
}